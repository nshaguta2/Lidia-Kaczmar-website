export interface NavItem {
  name: string;
  href: string;
}

export interface HeroImage {
  src: string;
  alt: string;
}

export interface Photo {
  id: string;
  src: string;
  alt: string;
}

export interface SocialLink {
  name: string;
  url: string;
}
